#ifndef FUNCTIONS_H_
#define fUNCTIONS_H_

#include <string>
#include <vector>
#include "person.h"

void EnterFirstName(char* arr, int size);
void EnterLastName(std::string& last_name_);
void Show(std::vector<Person>& vec, Show_t ShowF1, Show_t ShowF2);

#endif
