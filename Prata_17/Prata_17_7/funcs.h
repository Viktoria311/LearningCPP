#ifndef FUNCS_H
#define FUNCS_H

#include <fstream>
#include <string>
#include <vector>

void ShowStr(const std::string& s);
void GetStrs(std::ifstream& stream, std::vector<std::string>& vec);

#endif // FUNCS_H
